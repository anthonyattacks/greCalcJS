/* 
-- Use import / export module system via in-built folder structure
    -- subpoint: use require, use babel, use react in-built
-- Gain usage statistics with time, present using the tools built here-in
    -- subpoint: possible demonstration of python / linguistic similarity?

-- Add definitions for each function as return strings [ie. defExpo() {return "An exponent is...}"]

-- Consider rewriting probability functions in prolog
*/
